spring:
  application:
    name: movie-consumer-ribbon-list
server:
  port: 8012
eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://root:root@localhost:8761/eureka
  instance:
    prefer-ip-address: true